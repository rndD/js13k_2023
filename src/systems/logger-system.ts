import { System } from '@/utils/game-controller'

export class LoggerSystem extends System {
  update (elapsedFrames: number, totalFrames: number) {
    // console.log('passed frames %s', totalFrames)
  }
}
